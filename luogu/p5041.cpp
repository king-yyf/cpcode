#include <bits/stdc++.h>
using namespace std;
// https://www.luogu.com.cn/problem/solution/P5041


long long minSwapPalin(string &s) {
    deque<int> q(26);
    int n = s.size();
    for (int i = 1; i < n; ++i) {
        
    }
}


int main() {
    ios::sync_with_stdio(false); 
    cin.tie(nullptr);
    cout << fixed << setprecision(10);

   
    return 0;
}
